#include <stdio.h>
#include <string.h>
int main(void) {
    
    int len;
    char a[21];
    fgets(a, 20, stdin);
    len = strlen(a);
    int i;
    for(i = 0; i < len; i++) {
        
        printf("\'%c\'", a[i]);
        putchar(10);
    }
    
    return 0;
}
