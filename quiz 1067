#include <stdio.h>
int main(void) {
    
    int n;
    scanf("%d", &n);
    if(n < 0)
    printf("minus");
    else
    printf("plus");
    
    putchar(10);
    
    if(n % 2 == 0)
    printf("even");
    else
    printf("odd");
    
    return 0;
}
