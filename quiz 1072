        #include <stdio.h>
#include <stdlib.h>
int main(void) {
    
    int nx;
    scanf("%d", &nx);
    int *arr;
    arr = calloc(nx, sizeof(int));
    int i;
    for(i = 0; i < nx; i++) {
        
        scanf("%d", &arr[i]);
    }
    for(i = 0; i < nx; i++) {
        
        printf("%d\n", arr[i]);
    }
    
    return 0;
}
