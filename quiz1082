#include <stdio.h>
int main(void){
    
    int a;
    scanf("%x", &a);
    int i;
    for(i = 1; i < 16; i++){
        
        printf("%X*%X=%X", a, i, a*i);
        putchar(10);
    }
    
    return 0;
}
