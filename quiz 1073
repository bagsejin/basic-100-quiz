#include <stdio.h>
int main(void) {
    
    int arr[1000];
    int i;
    int count = 0;
    for(i = 0; i < 1000; i++) {
        
        scanf("%d", &arr[i]);
        count++;
        if(arr[i] == 0)
        break;
    }
    for(i = 0; i < count; i++){
        
        printf("%d\n", arr[i]);
    }
    
    return 0;
}
