#include <stdio.h>
int main(void) {
    
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    double sum = a * b * c;
    double result = sum / 1024.0 / 1024.0 / 8.0;
    printf("%.2lf MB", result);
    
    return 0;
}
