#include <stdio.h>
int main(void) {
    
    int a, b, c, d;
    scanf("%d %d %d %d", &a, &b, &c, &d);
    int i;
    i = a;
    int count = 1;
    while(count < d){
        
        count++;
        i = (i * b) + c;
    }
    
    printf("%d", i);

    return 0;
}
