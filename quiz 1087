#include <stdio.h>
int main(void) {
    
    int n;
    scanf("%d", &n);
    int i;
    int sum;
    for(i=0;i < n; i++) {
        
        sum += i;
        if(sum >= n)
        break;
    }
    printf("%d", sum);
    
    return 0;
}
